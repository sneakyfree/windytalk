# reference/ — THE PROTOTYPE (read-only)
The proven 2026-07-08 prototype, archived at tag `prototype-v0`. This is the PORT SOURCE for the
§3 ledger in docs/DNA_STRAND_MASTER_PLAN.md — port proven blocks out of here; never build on it,
never edit it. It still runs (needs its own .env, untracked).
