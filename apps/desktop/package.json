{
  "name": "windytalk-desktop",
  "version": "0.1.0",
  "description": "Windy Talk desktop voice client (Electron) — voice-session.v1",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.build.json && node --test dist/test/*.test.js",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^43.1.0",
    "typescript": "^5.6.3"
  }
}
