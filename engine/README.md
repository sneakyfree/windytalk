# engine/ — the voice brain-stem · **Python, server-side (GPU)**
STT/TTS provider ABCs (ADR-044), webrtcvad, the turn loop, the websocket server.
Speaks `contracts/voice-session.v1.md` to clients. Never contains client code.
