# wakeword/ — "Hey Windy" · **Python (training) + ONNX artifact**
openWakeWord custom pipeline: synth voices + negatives → train → validate ≥0.95 TP / near-0 FP
(release gate). Background track from Phase 0 on the 5090; stock hey_jarvis = dev-only fallback,
removed at release (ADR-058 D8).
